# Test Plan

1. list shows enabled models from settings fixture
2. enable adds model to provider list
3. disable removes model
4. search filters by provider/model name
5. existing unrelated settings preserved
